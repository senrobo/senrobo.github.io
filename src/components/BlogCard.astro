---
interface Props {
  title: string;
  description: string;
  pubDate: Date;
  url: string;
}

const { title, description, pubDate, url } = Astro.props;

const fullDate = pubDate.toLocaleDateString("en", {
  year: "numeric",
  month: "long",
  day: "numeric",
});
---

<li
  class="relative text-zinc-400 flex flex-col gap-2 sm:gap-3 sm:p-5 sm:hover:bg-zinc-800 transition-colors"
>
  <time
    datetime={pubDate.toISOString()}
    class="block text-sm tracking-relaxed text-zinc-400"
    transition:name="blogTime"
  >
    {fullDate}
  </time>
  <a
    class="text-lg before:absolute before:inset-0 font-semibold text-zinc-100"
    href={url}
    transition:name="blogTitle"
  >
    {title}
  </a>
  <p class="leading-relaxed max-w-prose">{description}</p>
</li>
